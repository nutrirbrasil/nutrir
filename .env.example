# Variáveis de ambiente ficam em cada app:
#   nutrir/.env.local  — site da marmitaria (Supabase, Telegram, InfinitePay, Pix)
#   pauli/.env.local   — site da nutricionista
#   nootr/.env.local   — app Nootr
#
# Veja os arquivos .env.example dentro de cada pasta.
